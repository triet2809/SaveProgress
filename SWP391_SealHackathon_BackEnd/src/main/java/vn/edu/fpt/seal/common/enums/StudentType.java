package vn.edu.fpt.seal.common.enums;

/**
 * Phân loại sinh viên tham gia.
 */
public enum StudentType {
    /** Sinh viên FPT. */
    fpt,
    /** Sinh viên ngoài (không thuộc FPT). */
    external,
    /** Không áp dụng / không phải sinh viên. */
    none
}
